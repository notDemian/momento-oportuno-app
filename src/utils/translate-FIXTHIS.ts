export const translateDEPRECATED = (text: string) => {
  return text
    .replace('Order for', 'Orden para')
    .replace('listing', 'anuncio')
    .replace('directory', 'directorio')
    .replace('microsite', 'micrositio')
    .replace('Paquete', '\nPaquete')
}
